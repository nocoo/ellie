# OSV-Scanner configuration
#
# All previously-suppressed advisories (undici/defu/cookie/esbuild/@hono/node-server)
# were resolved by `overrides` in package.json — patched versions are now installed
# directly. Keep this file as a placeholder for any future single-advisory waivers
# that genuinely cannot be patched.
#
# Format:
#   [[IgnoredVulns]]
#   id = "GHSA-xxxx-xxxx-xxxx"
#   reason = "Why this advisory cannot be patched today"
