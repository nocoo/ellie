// Admin content moderation page — placeholder

export default function AdminContentPage() {
	return (
		<div className="space-y-6">
			<h2 className="text-2xl font-semibold">Content Moderation</h2>
			<p className="text-muted-foreground">Content moderation — ViewModel ready, UI pending.</p>
		</div>
	);
}
