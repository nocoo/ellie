bunx lint-staged && bun run typecheck && cd apps/worker && bun test

# Rust (if cli-rs exists)
if [ -f "packages/cli-rs/Cargo.toml" ]; then
  cd packages/cli-rs
  cargo fmt --check --all
  cargo clippy --all-targets --all-features -- -D warnings
  cargo test --workspace
  cd ../..
fi
