// Version constant — single source of truth from root package.json
// Display format: "v1.10.2" (with "v" prefix for frontend/docs)

export const VERSION = "1.10.2";
export const VERSION_DISPLAY = `v${VERSION}`;
