{
	"name": "@ellie/test-mocks",
	"version": "1.2.5",
	"description": "Mock repositories for unit testing only - not used in production",
	"private": true,
	"type": "module",
	"exports": "./src/index.ts",
	"scripts": {
		"test": "vitest run -c vitest.config.ts",
		"test:coverage": "vitest run -c vitest.config.ts --coverage"
	},
	"dependencies": {
		"@ellie/types": "workspace:*"
	}
}
