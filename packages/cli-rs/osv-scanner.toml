# OSV-Scanner configuration for ellie Rust CLI workspace.
# Filters advisories whose trigger conditions are not reachable from this code.

[[IgnoredVulns]]
# lru@0.16.4 — potential UAF in LruCache::pop() under panic. Transitive via
# ratatui-core (^0.16 only); cargo cannot select ≥0.18.2 until upstream ratatui
# relaxes the range. Tracked: GH #522.
id = "RUSTSEC-2026-0253"
reason = "Transitive via ratatui-core which pins lru ^0.16; blocked until ratatui upgrades (GH #522)"
